{
  "name": "@sumi/api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/server",
    "migrate": "go run ./cmd/migrate",
    "build": "go build ./...",
    "lint": "go vet ./...",
    "test": "go test ./..."
  }
}
