{
  "name": "@sumi/agent",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build",
    "lint": "cargo clippy --all-targets -- -D warnings && cargo fmt --check",
    "check-types": "cargo check",
    "test": "cargo test"
  }
}
