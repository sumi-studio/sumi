// Long-lived agent runtime. The agent loop, 3-tier memory, and tool
// execution will live here (ported and adapted from pi).
// Domain operations must go through @sumi/api-client, never a direct DB.

function main(): void {
  console.log("sumi agent runtime: skeleton (nothing to do yet)");
}

main();
