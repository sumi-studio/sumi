// Re-export the agent-events contract types generated from
// `contracts/agent-events.yaml`. These are the authoritative wire types for
// agent events, commands, acknowledgements, and outbound frames.

export * from "./generated/agent-events";
